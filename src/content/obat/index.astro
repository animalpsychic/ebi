---
import { getCollection } from 'astro:content';
const items = await getCollection('obat');
---

<h1>Daftar Obat</h1>
<ul>
  {items.map(item => (
    <li>
      <a href={`/obat/${item.slug}`}>{item.data.title}</a>
    </li>
  ))}
</ul>
